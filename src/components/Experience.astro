---
import { experience } from "../data/experience";
---

<section
  id="experience"
  class="border-b border-line"
  aria-labelledby="experience-heading"
>
  <div class="mx-auto max-w-6xl px-5 py-16 sm:px-8">
    <h2
      id="experience-heading"
      class="font-serif text-3xl tracking-tight text-ink"
    >
      Experience
    </h2>
    <ol class="mt-10 space-y-0 divide-y divide-line">
      {
        experience.map((role) => (
          <li class="grid gap-6 py-10 lg:grid-cols-12">
            <div class="lg:col-span-4">
              <p class="font-mono text-xs tracking-wide text-muted uppercase">
                {role.start} — {role.end}
              </p>
              <h3 class="mt-2 font-serif text-2xl leading-snug text-ink">
                {role.title}
              </h3>
              <p class="mt-1 text-accent">
                {role.company}
                <span class="text-muted"> — {role.location}</span>
              </p>
            </div>
            <div class="lg:col-span-8">
              {role.summary && (
                <p class="mb-4 text-[1.02rem] text-muted">{role.summary}</p>
              )}
              <ul class="list-disc space-y-2 pl-5 text-[1.02rem] text-muted marker:text-accent">
                {role.bullets.map((item) => (
                  <li>{item}</li>
                ))}
              </ul>
              <p class="mt-5 font-mono text-xs leading-relaxed text-muted">
                <span class="text-ink">Environment: </span>
                {role.stack.join(" · ")}
              </p>
            </div>
          </li>
        ))
      }
    </ol>
  </div>
</section>
